'use strict';

/**
 * @ngdoc function
 * @name formidableApp.controller:RegisterCtrl
 * @description
 * # RegisterCtrl
 * Controller of the formidableApp
 */
angular.module('formidableApp')
  .controller('RegisterCtrl', function ($scope) {
    console.log($scope);
  });
